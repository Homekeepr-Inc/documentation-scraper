# --- Core service -----------------------------------------------------------
DOMAIN_NAME=localhost
TLS_DIRECTIVE=
LOG_LEVEL=INFO
SCRAPER_SECRET=replace-with-shared-secret

# Storage overrides (defaults defined in app/config.py)
BLOB_ROOT=./data/storage
DB_PATH=./data/manuals.db

# --- API keys ---------------------------------------------------------------
SERPAPI_KEY=replace-with-serpapi-key
TWO_CAPTCHA_API_KEY=replace-with-2captcha-key

# Optional AI providers for the legacy solver
OPENAI_API_KEY=
GOOGLE_API_KEY=
OPENROUTER_API_KEY=
AI_CAPTCHA_PROVIDER=gemini

# --- ManualsLib proxy strategy (see docs/manualslib_proxy_strategy.md) ------
PROXY_PROFILE_NAMES=proxy_a,proxy_b

# Profile A
PROXY_A_PUBLIC_URL=http://login:password@proxy-a.example.com:3128
PROXY_A_TYPE=HTTP

# Profile B
PROXY_B_PUBLIC_URL=http://login:password@proxy-b.example.com:3128
PROXY_B_TYPE=HTTP

# Local/dev fallback proxy (bypassed in Docker because start_with_proxy.py sets it)
PROXY_URL=http://localhost:8888
# TWO_CAPTCHA_PROXY=http://login:password@public-proxy.example.com:3128
# TWO_CAPTCHA_PROXY_TYPE=HTTP

# --- Captcha solver toggle --------------------------------------------------
CAPTCHA_SOLVER=2captcha
